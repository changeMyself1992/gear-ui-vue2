import BullyScreenIcon from './gear-bully-screen-icon.vue'
import './index.scss'

BullyScreenIcon.install = function (Vue) {
  Vue.component(BullyScreenIcon.name, BullyScreenIcon)
}

export default BullyScreenIcon
